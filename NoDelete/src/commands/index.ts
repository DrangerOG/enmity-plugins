import { logs } from "./logs"; // Import logs command

// Define the list of commands including the logs command
const commands = [logs];

// Export commands so they can be used elsewhere in the plugin
export { commands };
